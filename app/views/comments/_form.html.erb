<div class = "custom-form">
  <%= form_for ([@book, @comment]) do |f| %>
    <%= f.label :subject %>
    <%= f.text_field :subject %>

    <%= f.label :body %>
    <%= f.text_field :body %>

    <%= f.submit "Submit" %>
  <% end %>
</div>
